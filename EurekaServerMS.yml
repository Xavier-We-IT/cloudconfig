server:
  port: ${EUREKA_PORT}

spring:
  application:
    name: EurekaMS
  cloud:
    config:
      enabled: true

eureka:
  instance:
    hostname: ${EUREKA_INSTANCE}
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_DEFAULT_ZONE} 
